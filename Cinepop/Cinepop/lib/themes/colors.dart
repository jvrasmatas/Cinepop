import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Prevent instantiation — this is a static-only helper.

  static const Color midnightBlue = Color(0xFF0F172A);

  static const Color popRed = Color(0xFFFF334B);

  static const Color butterYellow = Color(0xFFFFD25D);

  static const Color white = Color(0xFFFFFFFF);

  // --- Derived / supporting shades ---------------------------------------

  static const Color surface = Color(0xFF1E293B);

  static const Color muted = Color(0xFF94A3B8);

  static const Color divider = Color(0xFF334155);
}
